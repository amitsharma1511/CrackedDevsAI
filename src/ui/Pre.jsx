import styled from 'styled-components';

const Pre = styled.pre`
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  font-size: 1.1rem;
  white-space: pre-wrap; /* Handles word wrapping */
`;

export default Pre;
